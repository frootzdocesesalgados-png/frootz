kind = "web"
previewPath = "/"
title = "Frootz - Doces & Salgados"
version = "1.0.0"
id = "artifacts/frootz"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24354

[services.development]
run = "pnpm --filter @workspace/frootz run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/frootz", "run", "build" ]
publicDir = "artifacts/frootz/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24354"
BASE_PATH = "/"
